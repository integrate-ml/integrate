export const registries: RegistryRegistry;
/** Registry of all types mod content can be. */
export const types: TypeRegistry;
import { RegistryRegistry } from "./registry.js";
import { TypeRegistry } from "./registry.js";
